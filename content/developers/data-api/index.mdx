import SwaggerUI from "swagger-ui-react"
import "swagger-ui-react/swagger-ui.css"

# Data API Playground

At a high level, the Velocity Data API is the backbone for reading historical and real-time protocol data so that apps, analytics, and integrations don't need to index the chain themselves. It follows the OpenAPI spec.

<Callout type="info">
The Data API is live at `https://data.velocity.exchange`. Try requests directly in the interactive playground at [data.velocity.exchange/playground](https://data.velocity.exchange/playground).
</Callout>

All endpoints are relative to `https://data.velocity.exchange`.

<SwaggerUI url="https://data.velocity.exchange/openapi.json" />