---
title: "Velocity SDK"
asIndexPage: true
---

`@velocity-exchange/sdk` is the TypeScript client for the Velocity program: it derives the accounts, builds the instructions, signs and sends the transactions, and keeps a live cache of the onchain state the caller reads between calls. These pages are written for someone integrating against it, whether that is a trading bot, a keeper, a front end, or a backend service. Everything here assumes the SDK; for the onchain layouts underneath it, see [Concepts](/developers/concepts).

## Where to start

Read [Setup](/developers/velocity-sdk/setup) and [Precision and Types](/developers/velocity-sdk/precision-and-types) before anything else. Setup covers constructing and subscribing a client; precision explains why every amount is a `BN` at a fixed exponent, which is the single mistake most likely to move real funds the wrong way. From there, [Deposits & Withdrawals](/developers/velocity-sdk/deposits-withdrawals) and [Orders](/developers/velocity-sdk/orders) cover the two paths almost every integration needs.

The rest is reference. Read a page when the thing it covers comes up.

## In this section

<Cards>
  <Card
    title="Setup"
    href="/developers/velocity-sdk/setup"
    description="Program IDs, the quote mint per environment, loading a wallet, and constructing and subscribing a VelocityClient."
  />
  <Card
    title="Precision and Types"
    href="/developers/velocity-sdk/precision-and-types"
    description="The precision constants, BigNum, token math helpers, and why a slot count is not a fixed amount of time."
  />
  <Card
    title="Deposits & Withdrawals"
    href="/developers/velocity-sdk/deposits-withdrawals"
    description="Moving tokens between a wallet token account and a subaccount's spot balance, plus the borrow and lend rates."
  />
  <Card
    title="Transfers"
    href="/developers/velocity-sdk/transfers"
    description="Moving deposits, borrows, and perp positions between subaccounts under one authority, including delegate transfers."
  />
  <Card
    title="Users"
    href="/developers/velocity-sdk/users"
    description="Subaccounts, the active subaccount, delegates, margin settings, and reading orders and positions off a User."
  />
  <Card
    title="Markets, Oracles, and Positions"
    href="/developers/velocity-sdk/markets"
    description="Reading perp and spot market accounts, oracle prices, market tier numbers, and the global state account."
  />
  <Card
    title="Orders"
    href="/developers/velocity-sdk/orders"
    description="Order types and post-only modes, placement and cancels, scale-order ladders, and the raw instruction builders."
  />
  <Card
    title="PnL & Risk"
    href="/developers/velocity-sdk/pnl-risk"
    description="Health, total and free collateral, margin requirement, leverage, unrealized PnL, and settling perp PnL."
  />
  <Card
    title="Events"
    href="/developers/velocity-sdk/events"
    description="The full event catalog and how EventSubscriber filters, buffers, and replays program events."
  />
  <Card
    title="DLOB"
    href="/developers/velocity-sdk/dlob"
    description="Building a local order book out of user accounts, then querying L2 depth and best bid/ask."
  />
  <Card
    title="Swaps"
    href="/developers/velocity-sdk/swaps"
    description="Routing a collateral swap through Jupiter or Titan so tokens move through the account's Velocity spot balances."
  />
  <Card
    title="Swift"
    href="/developers/velocity-sdk/swift"
    description="Signing an order offchain and submitting it for keepers and market makers to land onchain."
  />
  <Card
    title="Builder Codes"
    href="/developers/velocity-sdk/builder-codes"
    description="Attaching a builder fee to an order, the fill-time escrow requirement, and collecting accrued revenue share."
  />
  <Card
    title="Transactions"
    href="/developers/velocity-sdk/transactions"
    description="The four tx senders, blockhash caching, compute-unit sizing, and the priority-fee subscribers."
  />
  <Card
    title="SDK Internals"
    href="/developers/velocity-sdk/sdk-internals"
    description="Account subscription strategies, the subscriber and map catalog, caching behavior, and error handling."
  />
</Cards>

## End-to-end example

Connect, deposit collateral, place a market order, and read back the position. Each step is covered in depth on its own page.

```js
import { Connection } from "@solana/web3.js";
import {
  PositionDirection,
  VelocityClient,
  Wallet,
  getMarketOrderParams,
  loadKeypair,
} from "@velocity-exchange/sdk";

// 1. Connect and subscribe (see Setup)
const connection = new Connection("<RPC_URL>", "confirmed");
const wallet = new Wallet(loadKeypair("<KEYPAIR_PATH>"));
const velocityClient = new VelocityClient({ connection, wallet, env: "mainnet-beta" });
await velocityClient.subscribe();

try {
  // 2. Deposit 100 quote-asset units as collateral (see Deposits & Withdrawals)
  const quoteMarketIndex = 0; // spot market 0 is the quote asset
  const amount = velocityClient.convertToSpotPrecision(quoteMarketIndex, 100);
  const associatedTokenAccount =
    await velocityClient.getAssociatedTokenAccount(quoteMarketIndex);
  await velocityClient.deposit(amount, quoteMarketIndex, associatedTokenAccount);

  // 3. Place a market order: long 1 SOL-PERP (see Orders)
  const txSig = await velocityClient.placePerpOrder(
    getMarketOrderParams({
      marketIndex: 0, // perp market 0 is SOL-PERP
      direction: PositionDirection.LONG,
      baseAssetAmount: velocityClient.convertToPerpPrecision(1),
    })
  );
  console.log("order placed:", txSig);

  // 4. Read back account state (see PnL & Risk)
  const user = velocityClient.getUser();
  console.log("health:", user.getHealth());
} finally {
  await velocityClient.unsubscribe();
}
```

<Callout type="info">
Every SDK call that sends a transaction can fail: insufficient collateral, a stale oracle, an RPC error. Wrap calls in `try`/`catch` and inspect the program error code. See [Error handling](/developers/velocity-sdk/sdk-internals#error-handling) for how to decode program errors and retry safely.
</Callout>
