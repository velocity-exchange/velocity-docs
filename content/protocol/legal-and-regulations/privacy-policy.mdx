---
title: "Privacy Policy"
---

import CanvasText from "../../../components/CanvasText";

{/* The page's real title, from frontmatter above, already renders as the H1.
    This used to carry a second CanvasText block drawing "Privacy Policy"
    again as its own canvas-painted heading, directly under the real one. */}

<CanvasText
  width={600}
  mobileWidth={320}
  height={4800}
  mobileHeight={8900}
  fontSize={14}
  textContentUrl={process.env.NEXT_PUBLIC_S3_URL_PRIVACY_POLICY}
/>
