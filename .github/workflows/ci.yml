name: ci

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  lockfile:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .tool-versions
          cache: pnpm

      - name: Install dependencies (fails if pnpm-lock.yaml is out of sync)
        run: pnpm install --frozen-lockfile

      - name: Fail on a stray lockfile
        run: |
          if git ls-files | grep -E '(^|/)(package-lock\.json|yarn\.lock)$'; then
            echo "::error::pnpm-lock.yaml must be the only committed lockfile (see #73)"
            exit 1
          fi
