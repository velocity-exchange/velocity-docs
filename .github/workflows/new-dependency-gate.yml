name: New dependency gate

on:
  pull_request_target:
    types: [opened, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: write
  issues: write # v2 deletes a stale label through the issues API

jobs:
  new-dependency-gate:
    name: New dependency gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0 # required: the action diffs against the base commit
      - uses: velocity-exchange/shared-workflows/dependency-gate@d22dd0269eee2a156785b71d56b987ae7ae3e7b2 # dependency-gate-v2.0.0
        with:
          lockfiles: bun.lock
          require-pull-request-target: true
