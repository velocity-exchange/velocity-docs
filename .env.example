# copy to .env.local
NEXT_PUBLIC_S3_URL_TERMS_OF_USE=
NEXT_PUBLIC_S3_URL_PRIVACY_POLICY=
# Server-side only. Read by /api/onchain-data; must NOT be NEXT_PUBLIC_, or the
# endpoint (and any embedded API key) would be inlined into the client bundle.
RPC_URL=